# AGENTS.md

## 项目定位

- 本项目是 ComfyUI 图像选择器自定义节点。
- 工作流执行到节点时暂停，由用户选择继续传递的图像。
- 参考 `cg-image-filter` 的批次筛选能力与 `comfyui-image-picker` 的简洁交互，但不直接复制实现。

## 开发约定

- 保持 Python 节点逻辑与前端交互分离。
- 优先兼容 ComfyUI 现有工作流、批次语义和节点 API。
- 界面保持简洁，支持键盘操作，并正确处理取消、超时和多实例。
- 只引入必要依赖；行为变更需同步更新 `docs/`。
