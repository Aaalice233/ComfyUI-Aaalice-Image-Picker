# ComfyUI-Aaalice-Image-Picker

一个面向 ComfyUI 的人工图像筛选节点：在后续放大、细化或保存前暂停工作流，让用户从图像批次中选择需要继续处理的结果。

## 初步范围

- 单选与多选图像
- 选择后保持正确的 IMAGE 批次输出
- 取消与超时处理
- 清晰的选中状态、预览和键盘操作
- 同一工作流内多个选择器互不干扰

首版以稳定的核心选择流程为主，暂不扩展蒙版编辑、文本编辑等无关能力。
